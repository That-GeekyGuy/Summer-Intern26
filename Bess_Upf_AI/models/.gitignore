# Dataset files — large, regenerable from raw Prometheus data, never commit.
*.parquet

# STL/split debug artifacts — large, not needed at inference time.
split_report.json
training_report.md

# All other model artifacts (*.joblib, *.pt, *.json) ARE committed.
# random_forest.joblib (1.2 GB) is tracked via Git LFS — see /.gitattributes.
# Run: git lfs install && git lfs pull
